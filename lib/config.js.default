export const config = {
    path: {
        pfxTmpFolder: "/tmp/pk12/"
    }
}

export const couchbase = {
    cluster: "couchbase://127.0.0.1",
    buckets: {
        notabook: {
            name: "default",
            password: null
        }
    }
}

export const secret = {
    keyCert: "your-secret-goes-here"
}