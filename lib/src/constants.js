'use strict'

import path from 'path';

export const ERROR_PATH = 'Invalid path!';
export const EXT = '.xml';
export const STORAGE = path.resolve('./db');
export const LAST_DATE = 'date';
export const LAST_BATCH = 'batch';
export const DEFAULT_DELAY = 5;
